# Dev local — chargé automatiquement par Next.js en mode development
# Pour une base locale : docker compose up -d && npm run db:push && npm run db:seed

DATABASE_URL="postgresql://queenofex:queenofex@localhost:5433/queenofexcellence?schema=public"
AUTH_SECRET="dev-only-secret-change-in-production"
